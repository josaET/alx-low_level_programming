#include <stdio.h>

/**
 * main - A program that print the number of arguments passed int it.
 * @argc: argument count 
 * @argv: argument vector
 *
 * Return: always 0 (success)
 */

int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc = argc[1]);
	return (0);
}
